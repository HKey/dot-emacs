;;; config-autorevert.el --- autorevert              -*- lexical-binding: t; -*-

(require 'my-bootstrap)
(require 'autorevert)

(setq auto-revert-verbose nil)


(provide 'config-autorevert)
;;; config-autorevert.el ends here
